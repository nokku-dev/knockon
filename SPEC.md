# SPEC — knockon

> リポジトリ / プロダクト名: `knockon`（nokku 配下）
> ステータス: 設計改訂 v0.4（独立レビュー反映）/ 実装前
> prime objective: 収益最大化ではなく **「自分が毎日使う1本を最短で完成・出荷する」**。
> 関連: テンプレート / モジュール設計は [docs/template-modules-spec.md](docs/template-modules-spec.md)（Issue #62、 出荷後レイヤーの discovery / onboarding / 編集 UI）。

## 0. 完成判定（レビュー反映で変更）

- **N=1 完成 = Today で自分のチェーンの実運用ループが、実機で数日継続して回ること。**
- 旧定義（目標ビューで実運用が閉じる）は **ANT 違反のため破棄**（理由は §5）。
- 設計の精密化より**実機到達を優先**する（§ PLAN の早期検証ゲート参照）。

## 1. プロダクト定義

- 差別化: streak ではなく **If-Then / habit stacking（直前の行為が次の行為のトリガー）**。
- 根拠: Gollwitzer & Sheeran 2006（94 研究、d=0.65）。
- 設計思想: **Augmentation not Transformation**。煽らない・マイナスを指差さない・Celebrate 主。
- 既知の弱点（明示）: アナログで既に機能している自分の If-Then 運用を、デジタルが上回る理由は未検証。**デジタル版が現行運用より低摩擦であること**が成立条件。Phase 1 実機使用がこの前提の検証そのもの。

## 2. データモデル（v1 確定）

### チェーン構造

- **チェーン** = `起点アンカー` ＋ `ノードの順序列`。`status: active | stocked`。
- **起点アンカー** = チェーンに 1 つ。時刻 / 場所 / 行動のいずれか。**中間アンカーは持たない**（2 番目以降のトリガーは暗黙=直前ノード完了、UI 非表示）。
- **ノード** = v1 は **アクションのみ**。アクションは `バリアント` Null 許容（曜日/週マップ）。
- **サブチェーン参照は v1 非スコープ**（§5）。データ原則上、後から無移行で追加できる（理由は下記）。

### 達成・連鎖判定

- **達成記録** = `(ノード, 日付, bool)`。ノード達成側の唯一の正準データ。
- **アンカー発火イベント** = `(anchor_id, 日付)`。1 日 1 回の不可逆事実として記録（時刻/場所共通、[ADR-0012](docs/decisions/0012-anchor-firing-events.md)）。一度発火すれば範囲を出る・時刻を巻き戻るなどしてもその日中は発火済み扱い（Augmentation 原則と整合）。
- **手動メモ** = `(note_id, node_id?, content, created_at, updated_at)`（[ADR-0044](docs/decisions/0044-research-manual-notes.md)、#181）。ユーザーが書いた「観測した事実」軸（派生値ではない）。`node_id` は紐付け先ノード（= 特定チェーン内の特定アクション位置）。null = 汎用メモ。ノード削除時は `ON DELETE SET NULL` で本文を残し紐付けのみ外す（ユーザーの記録を消さない / Augmentation）。研究画面の右下 FAB（チェーン → アクション選択 or なし）と Today アクション長押し（対象ノード自動選択）の 2 導線から作成。表示時の「チェーン名 / アクション名」は保存せず派生で解決。
- **定着取り下げ** = `(node_id, retracted_at)`（[ADR-0047](docs/decisions/0047-settlement-lifecycle-and-log-portfolio.md)）。ユーザーが「最近やれてない」と感じたときに定着を取り下げる「観測した事実」軸（派生値ではない）。定着ステータス自体は保存せず派生（生涯マイルストーン latch）だが、取り下げ以降は再び実タップで定着バーを満たすまで育成中に戻る。システムは降格させない、本人だけが取り下げる。
- **日付の境界 = リセット時刻**（[ADR-0028](docs/decisions/0028-app-settings-and-reset-time.md)）。デフォルト `00:00`、設定 UI から `HH:MM` で変更可。例: `03:00` にすると 00:00-02:59 の操作は「昨日の日付」として記録される（夜型運用ユーザー向け）。Today / 分析 / メトリクスの「今日」は全て同じリセット時刻基準で統一。アプリ動作設定は別軸として `app_settings` シングルトン行に保持（観測データ軸ではない）。
- **保存は事実のみ、解釈は全て表示時の派生関数**。チェーン平均・連鎖の流れ・定着判定は非保存。
- **連鎖判定 = ゆるい**: ノードを飛ばしても各ノード独立達成。後続はゼロ評価にしない。
- **線（スパイン）の意味**: アンカーから **最後に達成済みのノード** まで `--grow`（明色）、それ以降は `--line-bg`。途中に未達ノードがあっても、両端が達成済みなら線は繋がる扱い（達成済みノード範囲モデル）。マーカー自体は各ノード独立にチェック状態を持つ（ゆるい連鎖判定との一貫性、[ADR-0010](docs/decisions/0010-spine-fill-to-last-achieved.md)）。
- 派生が正準でないため、サブチェーン等の拡張は将来ノード種別を足すだけで足り、データ移行が発生しない（この可逆性が v1 から外せる根拠）。
- 留意（未解決の前提）: ゆるい判定は Celebrate と整合する一方、連鎖を測らないことが実装意図（連鎖が遵守を生む）の便益を薄める可能性。Phase 1 実使用で観察する論点として残す。

### 定着の語彙（表示派生・v1）

- **左の SVG マーカー: 未定着は円 / 定着は星型** ([ADR-0050](docs/decisions/0050-settlement-star-marker-and-today-headline.md)、2026-07-07 で Issue #118「常に円」を反転)。定着ノードはドット自体を星 (常に塗り `★`) にし、達成 → 定着で「四角/円 → 星」に変わる様を見せる。未定着ノードは従来どおり円 (達成でバウンス)。星は今日の達成状態で塗り分け**しない** (Issue #113 の ★/☆ 塗り分けは撤回済み・(−) 禁則と整合)。アクション名右の小さな ★ (旧 #118 追補) は**撤去** (星は左ドットに一本化)。タップは定着ノードも従来どおり有効 (auto-✓ #211 は撤回・星は達成とは別軸のマイルストーン)。定着 = 星は Today のノードドットと見出し「定着 N 個」で示す（7D / 60D マトリクスは [ADR-0051](docs/decisions/0051-remove-matrix-and-merge-fresh-into-growing.md) で撤去）。
- **定着ライフサイクル（[ADR-0047](docs/decisions/0047-settlement-lifecycle-and-log-portfolio.md) / [ADR-0051](docs/decisions/0051-remove-matrix-and-merge-fresh-into-growing.md)）**: ノード単位で **育成中（未定着＝実タップ 0 も含む）→ もう少しで定着（現在の 14D 窓で達成 8〜9 日・定着バー直前）→ 定着（取り下げ以降に実タップで定着バー = 当面 14D 中 10 日 を満たした窓が存在）** の **3 ステージ**（ADR-0051 で旧「これから（実タップ 0）」を「育成中」に統合＝“未着手 backlog を指差す”ニュアンスを排す）。定着は**生涯マイルストーン（latch）で派生**（履歴の不変性から単調増加）。定着ノードは Today で **左ドットを星型**にして示し（[ADR-0050](docs/decisions/0050-settlement-star-marker-and-today-headline.md)）、**タップは従来どおり有効**（星は達成状態とは別軸のマイルストーン記号。auto-✓ #211 は撤回・達成レコードは実タップのみ・K-002 厳守）。ユーザーだけが「取り下げ」で育成中に戻せる（マイナスを指差さない / 判断権はユーザー）。取り下げ導線は **定着ノードの長押しメニュー（メモを追加 / 定着を取り下げる）と ログ画面の定着ポートフォリオ**の両方に置く（#205）。Today 見出しは「**定着 N 個**」（#213 の effective 累計は撤回）。
- 白抜き星（サブチェーン部分達成）は v1 非スコープに伴い**廃止**。
- 旧仕様 (v0.3) の「形変化は『円 → 星』の 1 回のみ」は Issue #118 で撤回 (= 左マーカーを目立たせると線の主役性が薄れる)。 祝福は派手にしない / 連続回数の近くに小さく置く / 反 streak の控えめトーンに合わせる。

## 3. ビュー（v1 は 3 種）

1. **Today**: 今日発火するチェーン/ノードの実行・記録。最頻滞在。**最重要・最優先**。
2. **チェーン**: 一覧（親）→ 詳細（拡大先）。詳細 = 起点アンカー＋ノード列の 1 本連続スパイン。
3. **一覧**: `active` / `stocked` フィルタ（ステータス切替で地続き）。
- **目標ビューは v1 非スコープ**（§5）。
- **分析（ログ）タブは v1 リリース表示**（[ADR-0047](docs/decisions/0047-settlement-lifecycle-and-log-portfolio.md) で #194 / [ADR-0045](docs/decisions/0045-exclude-analytics-tab-from-release-scope.md) の「非表示」を反転）: 中身を**達成率ダッシュボードから「定着ポートフォリオ」へ組み替え**たことで、リリースに載せる価値が生まれた。上部に「定着 N・もう少しで定着 P・育成中 M」カウント（クロス断面スナップショット・Celebrate 主役）、その下に「**今週の流入**」＝先週（7日前）から定着 / もう少しで定着へ**上方向に移動した個数**（`定着入り +X・もう少しで定着入り +Y`。増減ではなく流入数・降格は数えない。両方 0 の週は非表示）。本体はステージ別（定着 / もう少しで定着 / 育成中）グルーピング一覧。**各ステージ群は開閉可能**（見出しタップで開閉・件数は見出しに常時表示・0 件でも見出しは常時表示）で、**初期は全グループ畳んだ状態**（開閉状態はローカル UI 状態で永続化しない）。**達成率グラフ（= 比率）は撤去**（[ADR-0036 §一般原則](docs/decisions/0036-rescind-today-streak-display.md) の (−) 禁則に収束）。**60D 達成マトリクスは [ADR-0051](docs/decisions/0051-remove-matrix-and-merge-fresh-into-growing.md) で撤去**（未達セル＝ギャップを指差すため。`DateMatrixSection` / `useDateMatrix` のコードは残置・出荷後に復帰可）。メトリクス節（身体指標）は別軸で存置。リリース時のタブは Today / チェーン / ログ（定着ポートフォリオ）の **3 本**（研究タブは [ADR-0049](docs/decisions/0049-exclude-research-tab-from-release-scope.md) で `href: null` により非表示＝リリーススコープ外。ルート/コード・notes データは残置、出荷後に復帰可）。
- **研究タブ**（#175 で新設。**リリースでは非表示**＝[ADR-0049](docs/decisions/0049-exclude-research-tab-from-release-scope.md) で `href: null`。以下は再有効化時の仕様として残置）: 自動検出（インサイト）は未実装のため、当面は**ユーザーの手動メモ一覧 + 右下 FAB**（[ADR-0044](docs/decisions/0044-research-manual-notes.md)、#181）。メモは観測した事実軸（§2）。メモ削除は編集モーダル内の「このメモを削除」ボタン + `Alert.alert` 確認に集約する（#200、隠しジェスチャの発見性欠落解消 + Today アクション長押しの意味論（メモ作成）との対称化）。メモと自動インサイトの表示統合はインサイト実装時に別ソース併置で行い、メモテーブルにインサイトを混ぜない（派生値を保存しない / ADR-0001 と整合）。

### 進捗の見せ方（v1）

- ノード単位。1 本連続スパイン（起点が上、下へ流れる）。チェーン平均は派生表示の従。
- ウィンドウは **目的ごとに窓を持つ (v1、 [ADR-0038](docs/decisions/0038-today-chaindetail-7d-matrix-axis-reinterpretation.md) で軸別再解釈)**: **Today カード / チェーン詳細スパイン / 定着判定 = 14D** (標準窓)、**Today ChainDetail ノード行のミニマトリクス = 7D** (#125 / [ADR-0038](docs/decisions/0038-today-chaindetail-7d-matrix-axis-reinterpretation.md)、 実行直前の振り返り)、**分析タブの達成マトリクス = 60D** (#115 / [ADR-0037](docs/decisions/0037-analytics-date-matrix.md)、 中期俯瞰)。**ただし 7D / 60D のマトリクスは [ADR-0051](docs/decisions/0051-remove-matrix-and-merge-fresh-into-growing.md) で UI から撤去済み**（未達セル＝ギャップを指差すため。派生関数・コンポーネントは残置）なので、**現行で生きている窓は 14D と全期間（累計）の 2 つだけ**。7D / 60D は復帰させる判断をしたときに再び有効になる。**ユーザーが窓を切り替える UI (7/14/31 切替チップ等) は v1 非スコープ** — 窓の値は目的ごとに実装側の固定値として決まる (= 同じ目的に複数窓を持たない)。
- **⚠ 60D / 7D マトリクスは [ADR-0051](docs/decisions/0051-remove-matrix-and-merge-fresh-into-growing.md)（2026-07-07）で UI から撤去**（未達セル＝ギャップを指差すため・実機で「やらない日にテンションが下がる」）。以下の仕様はコード残置・出荷後に復帰する場合の参照として残す。
- **分析タブの達成マトリクス (#115 / ADR-0037、#128 で調整・ADR-0051 で撤去)**: 縦 = ノード（チェーン単位グループ）、横 = 日付（過去 60 日、最新が右、横スクロール、初期位置 = 右端）。セルは**塗り四角の二値**（達成 = グリーン塗り `COLOR_OK` / 未達 = 淡グレーのアウトライン / 休む日 = 空）。段階塗り率・赤・連続日数の数字や色強調は持たない（反 streak / Celebrate 主）。**表示専用**（タップ詳細は持たない。ノード状態はマトリクス自身が示すため）。旧「日々の詳細」（日付チップ列 + 1 日スナップショット、[ADR-0034](docs/decisions/0034-analytics-daily-detail-view.md)）はマトリクスと重複するため #128 で廃止（その日のメトリクス値の閲覧も廃止、トレンドグラフ + 手入力の MetricsSection は存続）。「格子 / ヒートマップ追加禁止」とは、禁止の趣旨（段階塗り率 / 赤 / streak 強調）を持たない二値マトリクスは対象外として両立（ADR-0037 で再解釈）。
- **Today アクション行の直近 7 日マトリクス (#125 / [ADR-0038](docs/decisions/0038-today-chaindetail-7d-matrix-axis-reinterpretation.md))**: Today の Bottom Sheet 内 ChainDetail の各ノード行の右端に、直近 7 日間（最新が右端）の達成グリフマトリクスを小さく表示する。セル語彙は分析タブのマトリクスと同じ二値（達成 = `COLOR_OK` 塗り / 未達 = `COLOR_FG_FAINT` アウトライン / 休む日 = 空）。タップ詳細・段階塗り率・連続強調・赤は持たない（反 streak / Celebrate 主との整合）。横スクロールはしない（7 セル固定）。今日のセルはノードタップによる楽観更新で即時反映する。実行直前の行内に過去 7 日が常に同居するため、Today 内で「次に何をするか」と「直近どこまで積めているか」を視線移動なしで把握できる。
- **連続達成日数 (streak) は表示しない (#123 で #103 を撤回)**: 一度 Today カード / ChainDetail ノード行に「N 日連続」を出した (#103) が、 反 streak / Celebrate 主 (DESIGN-SYSTEM §0 / ADR-0004) の核に照らして撤回した。「小さい数字なら Celebrate として許容」という #103 の判断は、 連続を煽り切れた時にマイナスを指差す方向に働くため不採用。 Today / 分析ビューとも streak の数字は計算も表示もしない。 達成の手応えは「積み上がる線 (スパイン)」と「定着 = 星 (★/☆)」だけで作る。
- **Today 見出しの「定着 N 個」 ([ADR-0050](docs/decisions/0050-settlement-star-marker-and-today-headline.md)、2026-07-07)**: Today 見出し右に `定着 N 個` 形式で表示。`N` = アプリ全体（active / 非 active 問わず全ノード）の定着ノード数（latch・派生・`countSettlementStages().settled`）。latch なので単調増加 (+)・ADR-0036 と整合。定着 0 は非表示（最初の定着から現れる）。**旧「累計 N 個達成 (+今日M)」（#142 / [ADR-0041](docs/decisions/0041-cumulative-count-revision.md) / #205 effective 化）は撤回**。実タップ通算（累計）は立ち上げチェックリストのマイルストーン判定にのみ内部利用（見出しには出さない）。窓を持たない（全期間固定）。
- **数字 / グリフ追加時の判定軸: 失われうるか (+/-)** (#174 / [ADR-0036 §一般原則](docs/decisions/0036-rescind-today-streak-display.md)): Today / 分析 / 通知などレンダリング層で新規に数字・グリフ・ハイライトを足すときは「その指標は失われうるか (= 減る方向に動きうるか)」で判定する。**(+) 失われない指標** (単調増加 / one-way フラグ / 塗り分けしないクロス断面) は採用可、**(−) 失われうる指標** (連続日数 / 比率 / 段階塗り率 / 達成状態反映の塗り分け) は追加禁止。グレーは (−) 側に倒す。過去判断の対応表は ADR-0036 §一般原則を参照。

## 4. 発火ロジック

- Today に並ぶノード = **active な全チェーンのノード** ([ADR-0020](docs/decisions/0020-deprecate-manual-firing-concept.md) で旧「手動発火」概念は廃止)。「Today を開く → 並ぶ → タップ達成」の日常フローで運用が成立する。 アンカー (時刻 / 場所 / 行動) は通知 + 「発火中」ピル表示のための分類軸で、 Today に表示するかどうかとは独立。
- 場所発火: OS 標準ジオフェンス（region monitoring）。**有料 API 不要**（[ADR-0003](docs/decisions/0003-firing-logic.md)）。場所登録は v1 は現在地のみ。**#301（Phase 1.6b）で実装済み** — `src/geofencing.ts` が active チェーンの場所アンカーを OS に登録し、到達時にアンカー発火を記録して通知を出す。登録すべき region の決定は純関数 `src/geofenceRegions.ts`。
- **二重の検出経路**: バックグラウンドの OS ジオフェンス（常時権限が要る）と、Today を開いたときの前景判定（`useTodayData.detectPlaceFiringByGps`）。どちらも同じ `(anchor_id, 日付)` を書き、ADR-0012 の「1 日 1 回の不可逆」で二重発火しない。**常時権限が拒否されていても前景判定は動く** = ADR-0003 の手動フォールバック。
- 制約: **iOS の同時監視は 20 件まで**（超過分は登録されず、`syncGeofences` が `droppedForLimit` で返す。キューイングは Phase 2）、Always 拒否時は通知が出ないだけ (Today 表示は不変)、精度/遅延は OS 任せ前提。

## 5. v1 非スコープ（出荷後レイヤー / 今回のレビューで拡大）

- **サブチェーン参照（再帰1段）＋ 付随する白抜き星表現**（実機使用で必要性を判断してから追加。データ原則上あとから無移行で足せる）。
- **ユーザーが窓を切り替える UI** (Today / 分析の窓選択チップ等)。窓の値そのものは目的ごとに固定 (§3 / [ADR-0038](docs/decisions/0038-today-chaindetail-7d-matrix-axis-reinterpretation.md) 参照)。
- サブスク有料機能（年額＋買い切り）／広告／複数デバイス同期／テーマ追加／弱い輪／ソーシャル。

> 削った機能は「不要」ではなく「実機で自分が使ってから要否を判断する」後送り。データ正準が `(ノード,日付,bool)` + `(anchor_id, 日付)` のみで派生が非保存のため、いずれも移行なしで後から足せる。

> **目標ビュー / メトリクス / 分析** はもともと v1 非スコープだったが、[ADR-0024](docs/decisions/0024-goal-view-analytics-phase-3-unified.md) で **Phase 3 (チェーン詳細) と統合して v1 範囲に取り込み** に方針変更。K-004 の ANT 違反リスクは「メトリクスは任意 + チェーンと疎結合 + 連携は read-only」で構造的に回避する。

## 6. デザイン

[DESIGN-SYSTEM.md](DESIGN-SYSTEM.md)（v0.2）参照。サブチェーン展開・白抜き星・ウィンドウ切替は v1 非スコープに伴い実装対象外（DESIGN-SYSTEM 側の記述は将来用として残置）。
