# SPEC — If-Then 習慣アプリ

> リポジトリ / プロダクト名: `knockon`（nokku 配下）
> ステータス: 設計改訂 v0.3 / 実装前
> prime objective: 収益最大化ではなく **「自分が毎日使う1本を最短で完成・出荷する」**。N=1 充足を完成判定とする。

## 1. プロダクト定義

- 差別化: streak（行為の連続）ではなく **If-Then / habit stacking（直前の行為が次の行為のトリガー）**。
- 根拠: Gollwitzer & Sheeran 2006 メタ分析（94 研究、d=0.65）。
- 設計思想: **Augmentation not Transformation**。煽らない・マイナスを指差さない・Celebrate（積み上がりと達成）を主にする。

## 2. データモデル（確定・最終 / v0.1 のリンク=アンカー×アクション型は廃止）

### チェーン構造

- **チェーン** = `起点アンカー` ＋ `ノードの順序列`。
- **起点アンカー** = チェーンに 1 つだけ。時刻 / 場所 / 行動のいずれか（例「起床」）。**中間アンカーは廃止**（2 番目以降のトリガーは「直前ノードの完了」＝暗黙・UI 非表示）。
- **ノード** = アクション or サブチェーン参照。**再帰は 1 段まで**。
- **サブチェーン** = 内部に独自のノード順序列を持つ。

### 達成・連鎖判定

- **達成記録** = `(ノード, 日付, bool)`。**唯一の正準データ**（旧「リンク」を「ノード」に改称、構造原則は不変）。
- **保存は事実のみ、解釈は全て表示時の派生関数**（チェーン平均・連鎖の流れ・定着判定・星種別は非保存）。
- **連鎖判定 = ゆるい**: ノードを飛ばしても各ノードは独立に達成扱い。後続がゼロ評価にならない。線は連続実行範囲を表すが、飛んだ先のノードも個別に点灯する。
- **サブチェーン達成（親視点）** = 内部いずれか 1 ノードでも実行で「達成」。

### 定着と星の語彙（表示派生）

- **円（白抜き）** = 定着前。
- **塗り星** = 単独アクションが定着 / サブチェーン全ノード定着。
- **白抜き星** = サブチェーン一部未達だが親チェーンでは達成扱い。
- アラート・弱い輪の指差し表示は持たない（**v1 で弱い輪は廃止**）。

## 3. ビュー（4 種）

1. **Today**: 今日発火するチェーン/ノードの実行・記録。最頻滞在。
2. **チェーン**: 一覧（親）→ 詳細（拡大先）。詳細 = 起点アンカー＋ノード列の 1 本連続スパイン。サブチェーンは**デフォルト展開**（記録ビューで畳まない）。
3. **目標**: メトリクス推移 × 対応チェーン達成の重ね表示。
4. **一覧**: `active` / `stocked` フィルタ（専用ビューにせずステータス切替で地続き）。

### 進捗の見せ方

- 詳細はノード単位で測る。各ノード/サブチェーンが独立に進捗を持つ。
- 線は 1 本の連続スパイン（起点が上、下へ流れる）。チェーン平均は派生表示の従。
- ウィンドウ切替 7D / 14D / 31D。母数はノード×日数で派生計算。

## 4. 発火ロジック

- 今日出すノード = 起点アンカーのロジック発火 ∪ **手動発火**（手動発火は v1 必須・行動アンカー成立の中核）。
- 場所発火: OS 標準ジオフェンス。**有料 API 不要**。場所登録は v1 は現在地のみ（地図検索は後発）。
- 制約: ジオフェンス上限のキューイング、Always 拒否時は手動フォールバック、精度/遅延は OS 任せ前提。

## 5. v1 非スコープ（出荷後レイヤー）

- サブスク有料機能（ハイブリッド: 年額サブスク + 買い切り）。
- 広告（転換レバー＝床、文脈トリガーが本命）。
- テーマ追加 / 複数デバイス同期 / ソーシャル。
- 弱い輪・分析の作り込み（方針転換で v1 から除外）。

## 6. デザイン

`docs/DESIGN-SYSTEM.md`（v0.2: モダン・ミニマル / ダーク既定 / 伸びる線が主役 / 星はセレブレーション専用）を参照。
