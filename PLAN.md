# PLAN — knockon 実装順序

> SPEC v0.4 に整合。原則: **設計を精密化せず、Phase 1 を最短距離で実機に出して自分で数日使う。** これが独立レビュー（自分の危険シグナル「実機で触れない段階で企画を膨らませる」/ K-003 / K-004）への構造的回答。

## 早期検証ゲート（最上位ルール）

**Phase 1 が実機で動き、自分が実際に数日使うまで、サブチェーン・目標ビュー・ウィンドウ切替・有料・同期などの追加設計を一切進めない。** 設計の精密化は前進ではなく消耗（K-001/K-003）。このゲートを PLAN の他のどの判断より優先する。

> **着手ゲート判定（Issue #75 / 2026-05-31）**: Phase 1 早期検証ゲートは [ADR-0022](./docs/decisions/0022-phase-1-completion-and-verification-operation.md) で **クリア済み**（Today が実機で数日回り、検証運用フェーズに移行。 [K-021 追記](KNOWLEDGE.md) が裏付け）。 よってテンプレート / モジュール作業（Issue #62、 #68-#74、 [docs/template-modules-spec.md](./docs/template-modules-spec.md)）の着手は **解禁**。 ゲート未了による保留は不要。

## Phase 0 — データ層（最小・1 回で固める）

- **チェーン**（status: active/stocked、起点アンカー＋ノード順序列）
- **ノード**（順序つき、v1 はアクションのみ）
- **起点アンカー**（タイトル必須／時刻・場所 Null 許容、場所=座標+半径）
- **アクション**（バリアント Null 許容＝曜日/週マップ）
- **達成記録**（`(ノード,日付,bool)`、唯一の正準データ）
- メトリクス／サブチェーンは作らない（v1 非スコープ）。
- 永続化は `expo-sqlite` でローカル正準（同期なし）。詳細は [ADR-0007](./docs/decisions/0007-expo-react-native-stack.md)。
- **Expo プロジェクト雛形の初期化**: `expo init`、`package.json` スクリプト群、`tsconfig.json`、`jest-expo`、`app.json` / `eas.json`、ディレクトリ構成（`app/` または `src/`）を Phase 0 で確定する。
- **完了条件**: 自分のチェーン1本をコード手投入で読み書きできる ＋ Expo プロジェクトの雛形・スクリプト群が確定している。

## Phase 1 — Today（最優先・ここで自分が使い始める＝N=1 完成判定の場）

- 今日発火ノードの解決（起点アンカー評価 ∪ 手動発火）。
- ノード単位チェックオフ。連鎖判定はゆるい。
- 1 本連続スパイン描画（その日の達成集合からの派生）。ウィンドウは 14D 固定。
- **実機ビルドを最短で用意し、自分の実チェーンを載せて毎日チェックを始める。**
- **完了条件 = N=1 完成判定**: Today が実機で動き、**自分が数日継続して実運用を回せている**。ここで「デジタル版が現行アナログ運用より低摩擦か」という SPEC §1 の前提を検証する。回らなければ設計ではなくこの前提を疑う。

### Phase 1 の PR 分割

「実機で起動して触れる」を **PR-1.1 の完了条件** に置き、UI の作り込みより先に通電を取る（K-003 への構造的回答）。

| # | PR タイトル | 完了条件 | 依存追加 | スコープ外 |
|---|---|---|---|---|
| **1.1** | `feat: Phase 1.1 — Expo Dev Build で実機到達 + シード Today 表示` | iOS 実機で Expo 起動 → シードチェーン1本のノードリストが表示される | `react-native-safe-area-context`（`expo-sqlite` は Phase 0 で導入済み） | チェックオフ・線・通知・RN コンポーネントテスト |
| **1.2** | `feat: Phase 1.2 — ノード単位チェックオフ` | タップで `recordAchievement` → 当日の達成状態が UI に反映 / 再起動後も保持 | `jest-expo` 並走（最初の RN コンポーネントテストとなるため・[ADR-0008](./docs/decisions/0008-test-strategy-ts-jest-bettersqlite.md) §決定3） | 線描画・手動発火 UI |
| **1.3** | `feat: Phase 1.3 — 1 本連続スパインの最小描画` | 当日の達成集合から線の塗り範囲を派生計算（domain 層）→ View でレンダリング。14D ウィンドウは API 受口だけ実装し描画は当日のみ | `react-native-svg`（最小・ADR 化候補） | 7/31 切替（v1 非スコープ）・形変化アニメ |
| **1.4** | `feat: Phase 1.4 — 手動発火 + DB 自動初期化` | アプリ起動時に DB 初期化＋シード未投入なら自動投入 / 一覧から発火するチェーンを選ぶ動線 | — | 自動発火 |
| **1.5** | `feat: Phase 1.5 — 時刻アンカー自動発火 + ローカル通知` | 起点アンカー (kind='time') の今日発火判定（純粋関数）+ ローカル通知スケジュール | `expo-notifications` | 場所発火 |
| **1.6** | `feat: Phase 1.6 — 場所アンカー（OS ジオフェンス）+ 現在地登録` | 現在地登録 UI（最小）/ ジオフェンス登録 / Always 拒否時の手動フォールバック | `expo-location` | 地図検索（v1 非スコープ・[ADR-0003](./docs/decisions/0003-firing-logic.md)）/ Always 再要求ループ・許可誘導 UI（ADR-0003 §「決定」第 5 項で禁止） |

### Phase 1 の判断ポイント

- **1.1 が最重要マイルストーン**: ここで「Expo Dev Build を iOS 実機にインストール → シードが見える」を実証する。**ここで詰まったら設計ではなく Expo 環境を疑う**（[ADR-0007](./docs/decisions/0007-expo-react-native-stack.md) を覆す判断材料）。
- **N=1 完成判定の発動点 = 1.4 完了時点**: 「自分のチェーンが手動で記録できる」状態になる。1.5/1.6 は便利化レイヤーで、**手動発火が確立していれば自動発火が落ちても運用は成立する**（[ADR-0003](./docs/decisions/0003-firing-logic.md) §「決定」第 3 項）。
- **早期検証ゲートの実装ポイント**: 1.1-1.4 完了 → 一旦止めて自分で数日使う → 不便な部分にだけ 1.5 / 1.6 / Phase 2 / Phase 3 を当てる。順序は再評価可能（場所発火を後ろに置いた理由は権限フロー・ジオフェンス上限・Always 拒否時のフォールバックなど考慮事項が多いため）。

### Phase 1 で発生し得る ADR 化候補

「先に固定」ではなく、各 PR 着手時に必要になったら立てる。**[ADR-0008](./docs/decisions/0008-test-strategy-ts-jest-bettersqlite.md) 等で既に手順が書かれている範囲で済めば新規 ADR は不要**。

- **ADR-0009 (案)**: `react-native-svg` の採用（PR-1.3 着手前）/ 代替: View 矩形による線表現
- **ADR-0010 (案)**: 状態管理の選択 — **発動条件: PR-1.4 完了後の実使用で `useState` + repository 直接呼び出しでは捌けない状態が出てきた場合のみ**。Zustand / Jotai / React Context / useState 継続のいずれを採るか。早期検証ゲート（[ADR-0006](./docs/decisions/0006-phase1-completion-and-scope-narrowing.md)）に従い「実使用で不便が出るまで立てない」

## Phase 1.7 / 1.8 — チェーン / アクション CRUD 前倒し（[ADR-0014](docs/decisions/0014-crud-phase-1-7-1-8-frontload.md)）

ADR-0006 の早期検証ゲートに対し、N=1 ユーザー判断で「編集・削除が必要なのは自明」として CRUD を前倒し。Phase 2 から繰り上げ。

- **Phase 1.7**: チェーン新規作成 + チェーン編集（タイトル / ノード並び替え + 追加）+ アクション編集（タイトル）。削除なし。`shouldSeed` 削除で起動時空状態に。
- **Phase 1.8**: チェーン / ノード / アクション削除 + `PRAGMA foreign_keys=ON` + `ON DELETE CASCADE` 全リレーション（[K-018](KNOWLEDGE.md) の構造的解決）。

## Phase 2 — 残り CRUD / アクションのバリアント等（実使用で不便だと判明した分だけ）

- **variant (曜日マップのみ、 週マップは後回し)** + **チェーンステータス active/stocked 切替 UI** を [ADR-0018](docs/decisions/0018-variant-phase-2-frontload.md) で Phase 2 から前倒し採用 (ADR-0014 同様、 N=1 判断)。
  - variant: シンプル string map 形式。 サブチェーン化 (出荷後レイヤー) の際は variant も再設計する受容判断。
  - active/stocked: 「一時休止」したいチェーンを削除せずに stocked へ。 チェーン一覧でフィルタ。
- 残りはここで:
  - 週マップ variant (隔週)
  - ジオフェンス登録キューイング ＋ Always 拒否時の手動フォールバックの強化
- 完了条件は実使用で判断（Phase 1.7-1.8 で CRUD 基本機能は揃うので、Phase 2 は補強レイヤー）。

## Phase 3 — チェーン詳細 + 達成率ダッシュボード + メトリクス ([ADR-0024](./docs/decisions/0024-goal-view-analytics-phase-3-unified.md))

ADR-0006 で「Phase 3 = チェーン詳細だけ」だったが、ADR-0024 で **目標ビュー / 分析 / メトリクス** を v1 範囲に取り込み、Phase 3 と統合実装する判断 (K-004 罠は「メトリクスは任意 + チェーンと疎結合 + 連携 read-only」で構造回避)。

| サブフェーズ | スコープ | PR |
|---|---|---|
| **3a** | チェーン詳細画面 (1 本スパイン + ノード単位進捗 + 定着で円→星 + 14D 固定) | PR-Z1 |
| **3b** | 達成率ダッシュボード (チェーン別 / ノード別 / 14D 期間、 streak は使わない) | PR-Z2 |
| **3c MVP** | メトリクス DB + 手入力 UI + 時系列表示 (チェーンとは疎結合) | PR-Z3a |
| **3c 本実装** | ~~外部の個人記録用 DB との連携 (read-only sync)~~ → [ADR-0052](./docs/decisions/0052-remove-notion-metrics-integration.md) で**撤去** (実装したが実使用されていなかった) | PR-Z3b |

派生クエリのみで実装は薄い (派生値非保存原則は [ADR-0001](./docs/decisions/0001-chain-data-model.md) 維持)。

**完了条件**: ノードごとの積み上がり + 14D 達成率 + メトリクス推移を 1 画面で確認でき、 組み替え判断ができる。

## v1 出荷判断ゲート

Phase 3 まででプロダクトとして自分の運用が閉じる。以降は出荷後レイヤー。

## 出荷後レイヤー（Phase 1 実使用の後に要否判断）

サブチェーン参照＋白抜き星 / ウィンドウ 7/31 切替 / サブスク / 広告 / 同期 / ソーシャル。いずれもデータ正準が `(ノード,日付,bool)` + `(anchor_id,日付)` + `(metric_key,value,recorded_at)` のみのため移行なしで追加可能。

> テーマ (Light / Dark / Auto 切替) は元々出荷後レイヤーだったが、 [ADR-0029](./docs/decisions/0029-theme-mode-light-dark-auto.md) で v1 に取り込み (= app_settings 軸 + ALTER 経路の枠組みが ADR-0028 で揃った時点で取り込みコストが極小化)。 個別コンポーネント refactor は follow-up PR で段階移行。

## 設計意図

- N=1 完成判定を Phase 4（旧・目標ビュー）から **Phase 1（Today 実機実使用）** へ前倒し。完成基準が「自分が使えている」になり、曖昧な完了条件（消耗ゾーン）を消す。
- Phase 0 のみ先に固める（データモデル確定済・手戻り無し）。UI は使いながら変わる前提で作り込まない。
