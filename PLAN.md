# PLAN — 実装順序

> 対象: `knockon`。SPEC.md v0.3 モデルに整合。
> 原則: **Today を最速で立てて自分の実運用をアプリに移す。** Phase 1 以降は「自分が毎日使っていて不便だから直す」モードに入り、完了判定が「自分が困らなくなった」になる。

## Phase 0 — データ層（土台・1 回で固める）

確定エンティティを永続化（派生は一切保存しない）。

- **チェーン**（`status: active | stocked`、起点アンカー 1 つ ＋ ノード順序列）
- **ノード**（順序つき。種別 = アクション or サブチェーン参照、再帰 1 段）
- **起点アンカー**（チェーンに 1 つ。タイトル必須／時刻・場所 Null 許容。場所 = 座標+半径）
- **アクション**（バリアント Null 許容 = 曜日/週マップ）
- **サブチェーン**（内部に独自のノード順序列）
- **メトリクス**（定義 + 記録。タイムスタンプつき・1 日複数可・チェーン/アクションに任意関連付け）
- **達成記録**（`(ノード, 日付, bool)`。唯一の正準データ）
- 永続化はローカル正準。Drive 同期は v1 では持たない。
- **完了条件**: 自分の今のチェーン 1 本（起点アンカー＋数ノード、サブチェーン 1 つ含む）をコードから手投入して読み書きできる。

## Phase 1 — Today（最優先・ここで自分が使い始める）

- 今日発火ノードの解決（起点アンカーの時刻/場所/曜日バリアント評価 ∪ **手動発火**）。
- ノード単位チェックオフ。連鎖判定はゆるい（飛ばしても各ノード独立達成）。
- 1 本連続スパイン描画（その日のノード達成集合からの派生）。サブチェーンは**デフォルト展開**。
- **完了条件**: 自分の実チェーンを Today に出し、明日からチェックを始められる。← **最重要マイルストーン**。

## Phase 2 — チェーン編集（運用に必須）

- チェーン CRUD。起点アンカー編集（タイトル必須＋時刻/場所、Null 許容のプログレッシブ開示。**場所は v1 は現在地登録のみ**）。
- ノードの追加・順序組み替え。サブチェーン作成（再帰 1 段）。
- アクションのバリアント編集（通常は単一、必要時のみ曜日/週マップ）。
- ジオフェンス登録のキューイング ＋ Always 許可拒否時の手動フォールバック。
- **完了条件**: Phase 1 で手投入したチェーンをアプリ内だけで組み替えられる（コード投入が不要に）。

## Phase 3 — チェーン詳細

- 1 本スパイン。ノード/サブチェーンが独立に進捗を持つ。星 3 語彙（塗り星/白抜き星/円）は派生表示。
- ウィンドウ 7D / 14D / 31D 切替（母数 = ノード×日数で派生）。
- **弱い輪・アラート表示は持たない**（方針転換で v1 廃止）。派生クエリのみで実装は薄い。
- **完了条件**: リンクごと（ノードごと）の積み上がりを見て組み替え判断ができる。

## Phase 4 — 目標ビュー（**N=1 完成判定点**）

- メトリクス記録入力（1 日複数可）。
- メトリクス推移 × 関連チェーン達成の重ね表示。
- v1 はアプリ内手入力のみ（fitness / Body Metrics 連携は持たない）。
- **完了条件**: 「アクションは続いてるのに指標が動かない/動いた」を見て組み替えられる。**ここで実運用がアプリ上で完全に閉じる = 出荷判断可。**

## Phase 5 — 一覧／ストック

- チェーン一覧（親）→ 詳細（拡大先）の親子。`active` / `stocked` フィルタ。
- ストックは専用ビューにせずステータス切替で地続き。
- **完了条件**: someday 相当のチェーンが死蔵せず管理できる（完成後の快適化）。

## v1 非スコープ（出荷後レイヤー）

サブスク有料機能（年額＋買い切り）／広告（転換レバー＝床）／複数デバイス同期／テーマ追加／弱い輪・分析の作り込み／ソーシャル。N=1 完成判定には 1 つも不要。

## 設計意図

- N=1 完成判定は Phase 4 終了時点。Phase 5 は快適化なので Phase 4 で出荷判断してよい。
- Phase 0 のみ先に固めるのは、データモデルが確定し手戻りリスクが無いから。UI 側は使いながら変わる前提で作り込まない（v0.1→v0.3 の作り直しの教訓 = KNOWLEDGE K-003）。
