# Claude Code - 個人設定（git管理外）
CLAUDE.local.md
.claude/settings.local.json

# Dependencies
node_modules/

# Build / Cache
.expo/
dist/
web-build/
*.tsbuildinfo

# Native
ios/
android/

# Local DB / Misc
*.db
*.db-journal
.DS_Store

# Secrets (PR-Z3b: Notion API token 等は .env から process.env 経由で読む。 commit しない)
.env
.env.local

