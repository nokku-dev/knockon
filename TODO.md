# TODO

コンテキストがリセットされても作業を再開できるようにするためのタスク管理。
Claude Code のセッションをまたぐ作業の状態を記録する。

## 進行中

<!-- 今やっている作業。中断した場合ここを見れば再開できる状態にする

### タスク名
- **Issue**: #XX
- **ブランチ**: feature/xxx
- **現在地**: 何をどこまでやったか
- **次のステップ**: 再開したら最初にやること

-->

## iOS は Android 版 v1 出荷判断の手前までスルー

- **方針**: iOS の起動確認 / 通電は **Android で v1 (Phase 1 + 必要に応じて Phase 2/3) が出荷可能と判断できる手前** までやらない。各 PR 完了時に iOS でも動かす方式 / Phase 1 完了時点でまとめて確認する方式どちらも、iOS 固有の問題に時間を取られて Android の time-to-device / 実機運用ループが遅くなることが分かった (Phase 1 完了時点でユーザー判断、2026-05-19)。
- **着手タイミング**: Android で「自分の実チェーンが数日継続して回せている / v1 出荷判断ゲート手前」になった時点で iOS 通電をまとめて行い、出てきた問題は別 PR で対処する。
- **暗黙の前提**: コードベースは Expo (React Native) なので iOS は基本的に同じ JS バンドルで動く想定。実機固有の挙動差 (権限フロー / SafeArea / ナビゲーション等) は通電タイミングで初めて確認する。

## Phase 1.5b: EAS Dev Build 移行 + ローカル通知の正規実装

PR-1.5 (時刻アンカー) で `expo-notifications` を Expo Go に入れた瞬間に SDK 53+ の制限に当たった。push 通知は完全 removed、ローカル通知も非公式扱い。K-008 が予測した「Phase 1.5/1.6 で時刻 / 場所アンカーを実装した後に SDK 強制移行が来る」がそのまま発現。

- **PR-1.5 (本 PR) のスコープ縮小**: 時刻アンカー DB 保存 + Today 発火中ピル (派生表示) のみ。通知は実装せず。
- **Phase 1.5b (後発 PR)**: EAS Dev Build に降りる ADR (ADR-0012 候補) → `expo-notifications` 復活 → ローカル通知スケジュール + 通知許可フローを正規実装。
- **着手タイミング**: Phase 1.6 (場所アンカー) も Expo Go 制約 (`expo-location` の Always 許可周り) で同じ判断点に到達する可能性が高い。**Phase 1.6 着手前 or PR-1.6 で同じ問題に当たった時** が Dev Build 移行判断の妥当なポイント。
- **Phase 1 全体 (Today + チェーン一覧 + 時刻発火中ピル + 場所アンカー UI) は Expo Go で実機検証可能**。通知 / 自動発火の OS 側機能は Dev Build 移行後に乗せる。

## Phase 1 実機運用中に観察する CRUD 着手シグナル

Phase 1 はチェーン / アクションの CRUD を含まない（シードチェーン 1 本固定）。ただし「Today 回す検証で必要になれば欲しい」という方針なので、実機運用中に以下のシグナルが出たら CRUD 着手を判断する。

- **A. シード書き換え + 再ビルド が週 2 回以上** → Phase 2 前倒し着手
- **B. 「このノード今日は飛ばしたい / 順序変えたい」を実機操作中に思う** → 編集 UI 最小実装 (並び替え + 削除のみ) を mid-Phase 1 で追加判断
- **C. 検証チェーンが 1 本では足りず別チェーンを足したくなる** → チェーン新規作成だけ先行実装（編集は後送り）
- **D. アンカー（時刻 / 場所）を毎週変えたくなる** → PR-1.5/1.6 でカバー済みなのでスコープ通り進める

逆に **シード書き換えだけで 2 週間以上回せている → CRUD 不要のまま Phase 1 完了** に倒す（早期検証ゲート [ADR-0006](docs/decisions/0006-phase1-completion-and-scope-narrowing.md) 遵守）。

## 完了

<!-- 直近の完了タスク。/retro の振り返り対象 -->
