# DESIGN-SYSTEM — v0.2

> ステータス: **v0.2 / 変更前提**。初期から質を確保する。
> v0（Quiet Ink / 紙・インク・editorial）は破棄。毎日使って気分が上がる **モダン・ミニマルツール** へ転換。
> 参照実装: `/reference/today.html`（v0.1・要 v0.2 化）, `/reference/chains-list.html`, `/reference/chain-detail.html`（v0.3 モデル反映済み）。

---

## 0. コンセプト（変えない核）

習慣アプリは「ストリークの格子」。本アプリの主役は **積み上がる線**。

- 格子 / ヒートマップ / 紙吹雪 / ストリーク炎は**使わない**。
- チェーン = 起点アンカーから下へ流れる **1 本の連続スパイン**。
- 達成 = 線が伸びること。マイナスは可視化しない（アラート・弱い輪の指差しなし）。
- **Celebrate 主**: 積み上がりと「定着＝星」だけで手応えを作る。
- konkon（ノック）はモーションに残す: 達成で線が一段スッと伸びる（波紋ではない）。

## 1. カラートークン

> ADR-0029 (Issue #53): Light / Dark / Auto (OS 追従) 切替を v1 で採用 (= 旧「Light テーマ / 動的テーマ切替は v1 非スコープ」方針を覆し)。 デフォルト Auto。 設定 → テーマカラーで選択。 パレット定義は `src/theme.ts` の `DARK_PALETTE` / `LIGHT_PALETTE` が正本。 個別コンポーネントは段階移行で `useTheme()` 経由化する (= 本 ADR §非スコープ、 follow-up PR 列)。

| トークン | 役割 | Dark（既定） | Light |
|---|---|---|---|
| `--bg` | 背景 | `#16161A` | `#F6F6F4` |
| `--surface` | カード面 | `#1E1E24` | `#FFFFFF` |
| `--line-bg` | 線の地（未達） | `#2A2A32` | `#E7E7E3` |
| `--fg` | 主テキスト | `#F4F4F2` | `#1A1A19` |
| `--fg-soft` | 副次テキスト | 52% | 52% |
| `--fg-faint` | 最弱・補助テキスト | `#8A8A90` | `#6E6E6E` |
| `--grow` | **伸びる線（主役）** | `#EAEAE8` | `#1A1A19` |
| `--accent` | 今日発火中 / ノック / destructive action (削除ボタン文字色のみ・背景はニュートラル) | `#E0574C` | `#B23A3A` |
| `--star` | 定着の星（セレブレーション） | `#F2C14B` | `#C9941F` |

原則: 達成は赤で煽らない。線は**ニュートラル明色のグラデで伸ばす**。accent は「今日発火中」と「ノック」、加えて **destructive action (削除確認ボタン)** に限定する。「マイナスを指差さない」原則と「不可逆操作の明示」を両立するため、destructive action では文字色のみ accent を使い、背景は LINE_BG に抑えて派手さを避ける (PR-1.8a)。star は定着到達時のみ。

> a11y (#74 / SPEC §7): `--fg-faint` は補助テキストにも使うため WCAG AA (4.5:1) を bg / surface 双方で満たす値に確定した（旧 Dark 28% / Light 26% は約 2.7:1 / 1.7:1 で未達だった）。コントラスト不変条件は `theme.test.ts` で機械検証する。

## 2. タイポグラフィ

- v0 の Fraunces（セリフ/インク）は破棄。**モダンサンセリフで統一**。セリフは使わない。
- 実装候補: 欧文 Geist / Hubot Sans、和文 Noto Sans JP / LINE Seed JP。参考 HTML は Hanken Grotesk + Noto Sans JP（仮）。
- 見出しは太く詰める（`font-weight:700`, `letter-spacing:-.025em`）。数値は tabular-nums。

## 3. スペーシング / 形

- 基準 4px。角は**ピル/大きめ丸**（`--r`=14px、ボタン/タグは 999px）。
- 影なし・罫線で仕切らない。階層は面（surface）と余白で作る。
- ダーク既定。Light も常時提供（朝晩運用 + 好み）。

## 4. シグネチャ要素

### 4.1 スパイン（チェーン）

- 起点アンカーが上端、ノードが下へ。**1 本の連続線**（分割ティックは使わない）。
- 伸び = アンカーから **最後に達成済みのノード** まで `--grow`、それ以降は `--line-bg`。途中に未達ノードがあっても両端が達成済みなら線は繋がる扱い（達成済みノード範囲モデル、[ADR-0010](docs/decisions/0010-spine-fill-to-last-achieved.md)）。表示時に派生計算する（保存しない）。
- サブチェーンは 1 段インデントした内側スパインを持ち、**デフォルト展開**。

### 4.2 マーカー（ノード状態の語彙）

| 記号 | 意味 |
|---|---|
| 白抜き円 | 定着前（達成があれば枠が `--grow`） |
| 塗り星 | 単独アクション定着 / サブチェーン全ノード定着 |
| 白抜き星 | サブチェーン一部未達・親では達成扱い |
| 起点ドット（塗り） | 起点アンカー |
| 横棒「—」(マーカー無し) | variant null の休む日 (Today のみ、 グレー表示 / タップ不可、 [ADR-0018](docs/decisions/0018-variant-phase-2-frontload.md)) |

形が変わるのは「円 → 星」の 1 回のみ＝定着のセレブレーション。塗り率の段階表現は持たない（円は常に白抜き）。「—」は「ドット無し」かつ「曜日設定でこの日は休む」明示用で、 形変化ではなく**情報補助**として扱う。

### 4.3 ノック（モーション・Celebrate 主役）

達成時のモーションは「**1 つの達成ジェスチャ**」として 3 パーツが同時発火する 1 セットで構成する:

1. **ノック (線が一段スッと伸びる)**: スパインの明色 (`--grow`) 部分が達成済みノードまで伸びる。`cubic-bezier(.22,1,.36,1)` / 320ms。
2. **マーカーバウンス**: タップした該当ノードのマーカー (円) が `scale 1 → 1.25 → 1` でバウンス (up 80ms / down は spring damping 8 / stiffness 200 で揺り戻し感あり)。
3. **テキスト拡大縮小**: タップした該当ノードのアクション名テキストが `scale 1 → 1.08 → 1` で**1 回だけ**伸縮 (up 120ms / down 240ms、両方とも同じイージング、揺り戻しなし)。マーカー (up 80ms) より約 1.5 倍ゆっくりで穏やかに伸縮。理由: 文字は read 対象なので spring の揺り戻しがあると視認性が落ちる + 同期させると速すぎて読み取りづらい (PR-1.9 ユーザーフィードバック)。倍率はマーカーより控えめ (1.08) で可読性を優先しつつ中央視野フィードバックを補強。

3 パーツとも **達成 (false→true) 遷移のみ発火**、達成解除 (true→false) では bounce しない (マイナスを指差さない)。

マーカー / テキストバウンスは Phase 1 実機検証で「線だけの祝福だと視線位置とフィードバック位置がずれて手応えが薄い」シグナルから追加 (PR-1.9 / [ADR-0006 早期検証ゲート](docs/decisions/0006-phase1-completion-and-scope-narrowing.md))。

「マイクロインタラクションを増やさない」原則の捉え方: マーカー / テキスト / ノックは**ユーザーから見て 1 つのジェスチャ**なので、構成パーツが増えてもユーザー体験としてはノイズにならない。逆に「祝福以外の場面に動きを増やす」(loading spinner の派手化 / hover エフェクト / ページ遷移エフェクト等) は引き続き禁止。`prefers-reduced-motion` で簡略化。

## 5. 変更時の規律

- 生 hex/px を直書きしない（トークン経由）。
- accent / star の用途を増やさない（増やしたくなったら、まず線の長さで表せないか検討）。destructive action は PR-1.8a で例外として追加済み (文字色のみ・背景は LINE_BG)。
- 格子・ヒートマップ・炎・紙吹雪・弱い輪の指差し表示は**追加禁止**（反 streak / Celebrate 主の核に反する）。
- 形変化は「定着で星」の 1 回だけに保つ。

---

### 参考アプリの位置づけ

`/reference/` 配下は v0.2 方向の参照。`chain-detail.html` は新チェーンモデル（起点アンカー＋ノード列・1 本スパイン・サブチェーン展開・星 3 語彙）反映済み。`today.html` は v0.1 のままで v0.2 化が未了。参照アプリ自体は出荷物ではない。
