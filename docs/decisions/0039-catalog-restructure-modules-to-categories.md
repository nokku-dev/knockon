---
id: 0039
date: 2026-06-17
project: knockon
tags: [architecture, data-model, ux, scope]
status: accepted
supersedes: []
superseded-by: []
---

# カタログを「モジュール(テーマ束)」から「カテゴリ2型(ジャンル別／おすすめ)」へ再構成し、採用単位を個別アクションにする

## 文脈

テンプレート機能の v0 カタログは「扉 → 束 → モジュール → リンク」の階層を持つモデルだった（[docs/template-modules-spec.md](../template-modules-spec.md) §2-§9 / [ADR-0030](0030-template-module-link-data-model.md)）。`module`（テーマ束）が朝/夜などの `moment` と `goal` を持ち、その中に `link`（個別アクション）がぶら下がる構造で、採用は束（モジュール）単位を基本に考えていた。

Issue #151（テンプレ化と採用時評価軸フィルタ設計）の triage/dialog（2026-06-17）で、この構造に 2 つの問題が確認された:

1. **採用前メタの方向が Issue の本質と逆だった。** 当初は監査（#121 / PR#127, [docs/template-audit.md](../template-audit.md)）の評価軸（一意性 / エビデンス可視性 / 網羅性）を活かし、各 link に「エビデンス属性」カラムを足してカタログ側メタが既定集合を決める案を出した（別途起票した子提案）。しかしこの提案は **環境由来（実 export）を既定に優先する**設計で、Taku の意図は **逆＝環境依存を既定から外し汎用のみ残す**だった。提案はお蔵入り。
2. **モジュール(テーマ束)が moment を抱える構造が、採用体験と噛み合わない。** ユーザーは「朝の束」を丸ごと採るより、ジャンル（運動・スキンケア等）から個別アクションを拾い、moment（朝/夜）は自分のチェーン側で決めたい。束に moment を固定で持たせると、同じ「歯磨き」が朝モジュールと夜モジュールに重複定義され、棚卸し・一意性の管理コストが増える（[K-029](../../KNOWLEDGE.md) / [K-030](../../KNOWLEDGE.md) の「X を充実させる」「軸の混在」と同型）。

[ADR-0030](0030-template-module-link-data-model.md) が確立した「catalog（テンプレ定義）と live（採用後の実チェーン）をテーブルで分離する」原則は維持したい（正準データ不変条件 [ADR-0001](0001-chain-data-model.md) と整合）。論点は **catalog の内部構造（module 概念と採用単位）をどう作り直すか**であり、catalog/live 分離そのものは覆さない。

本 ADR は **構造を固定する設計 ADR** であり、最終スキーマ DDL や seed 内容そのものの確定は後続の実装トラック（catalog/schema 再構成）の責務とする（Issue #153 の完了条件 = ADR 起票 + catalog schema と採用モデルの方針が固定される）。

## 検討した選択肢

- **案A（不採用）: module(テーマ束)モデル維持 + エビデンス属性カラム追加**
  - [ADR-0030](0030-template-module-link-data-model.md) の `modules` / `links` を保ち、`links` に評価軸（エビデンス可視性等）のメタを足して既定集合を決める（別途起票した子提案）。
  - 問題: 環境由来（実 export）を既定優先する方向で、「環境依存を既定から外す」という Issue の本質と逆。テーマ束に moment を固定する構造の重複問題も残る。

- **案B（採用）: module 廃止 → カテゴリ2型 + 採用単位＝個別アクション**
  - テーマ束（module）を廃止し、catalog を 2 型のカテゴリで構成する:
    - **ジャンル別カテゴリ**: 個別アクションを genre（運動・スキンケア等）で探す。各アクションは catalog 上 1 つだけ存在し、**moment（朝/夜）を持たない**（moment は採用後にユーザーのチェーン側で決まる）。
    - **おすすめカテゴリ**: 朝/夜の完成ルーティン束。ジャンル別カテゴリのアクションを**重複参照・順序つき**で並べた、ゴール提示用のプリセット。
  - **採用モデル = 個別アクション選択。** カテゴリを選んだときは初期全選択（外したいものだけ外す）。アクションの重複採用 OK。
  - 既定テンプレを汎用のみに棚卸し（環境依存／ニッチは完全削除、学習系カテゴリを新設）。

- **案C（不採用）: カタログをやめてユーザー全自作にする**
  - 採用前メタも束も持たず、空から作らせる。Augmentation 原則（初日に折れさせない / [K-031](../../KNOWLEDGE.md)）に反し、テンプレ機能の存在意義（#62）を否定するので不採用。

## 決定

**案B を採用。** [ADR-0030](0030-template-module-link-data-model.md) の **catalog/live 分離原則は継承**し、catalog 内部の **module(テーマ束) 概念を「カテゴリ2型」に置換**する。

### 構造

- **module(テーマ束)は廃止。** カタログは 2 型のカテゴリで構成する。
- **ジャンル別カテゴリ**: 個別アクションを genre で探す。各アクションは catalog 上 1 つ。moment は持たず、moment は採用後にチェーン側で決まる。
- **おすすめカテゴリ**: 朝/夜の完成ルーティン束。ジャンルのアクションを重複参照・順序つきで並べる（プレビュー＝ゴール提示）。
- **採用単位 = 個別アクション選択。** カテゴリ選択時は初期全選択。アクション重複採用 OK。

### v0 カタログ内容（方針固定 / 実装トラックで seed 化）

**ジャンル別カテゴリ（9）** ※「(任意)」= 初期選択から外す候補（**#191 追補**: 「任意」ラベルは意味が伝わりにくいためテンプレピッカー / discovery プレビューでは非表示化。`defaultOn=false` アクションは UI 上で表示されず、DB 列は保持 [K-005 双方向リンク]）:

- 水分・健康: 歯磨き / 水を飲む / 服薬 / 体重計(任意) / サプリ(任意)
- スキンケア: 洗顔 / スキンケア / 日焼け止め(任意)
- 運動: ストレッチ / ウォーキング / 筋トレ(任意) / プロテイン飲む / 軽い散歩
- 食事: 食事準備 / 食事
- 身支度: シャワー / 着替え / 髪を整える(任意) / 髪を乾かす(任意)
- 家事: 掃除 / 洗濯 / ゴミ捨て / 食器洗い / 食器しまう(任意) / 洗濯物畳む(任意)
- 内省・計画: ジャーナル / 今日やること / 読書 / 瞑想
- 入浴: 入浴
- 学習（新設）: 勉強 / 語学 / オンライン講座・動画学習 / 復習・ノートまとめ

**おすすめカテゴリ（2）**:

- 朝のおすすめ: 歯磨き→洗顔→スキンケア→水を飲む→ストレッチ→食事準備→食事→今日やること
- 夜のおすすめ: 食事準備→食事→食器洗い→入浴→スキンケア→髪を乾かす→読書→ジャーナル→歯磨き

**削除（汎用のみ残す棚卸し）**: コーヒー(淹れる) / 白湯 / お湯を沸かす / 食器を水につける×3 / プロテイン作る / 5分掃除 / 風呂を沸かす(入浴準備) / 深呼吸。朝/夜で重複していたアクション（歯磨き / 洗顔 / ストレッチ / ジャーナル / 食事）は各 1 つに統一。

### スコープ境界

- 本 ADR は**構造と採用モデルの方針を固定する**。最終スキーマ DDL（categories テーブルの形・おすすめカテゴリの順序つき重複参照をどう持つか・live ノードの由来参照を category にするか action にするか）と seed 実装は**後続の catalog/schema 再構成トラックに委譲**する。
- onboarding（[ADR-0031](0031-onboarding-first-run-route.md) / [ADR-0035](0035-onboarding-action-selection.md)）・discovery・編集 UI のチップ（[ADR-0032](0032-edit-ui-two-layer-chips.md)）は module 概念に依存しているため、本構造変更の波及を受ける。内容（v0 カタログ）を固めてから別トラックで対応する。
- 旧エビデンス属性提案（別途起票した子提案）はお蔵入り。復活させない。

## 理由

- **「環境依存を既定から外し汎用のみ残す」という Issue の本質に合わせた。** 実 export 由来の環境依存アクション（コーヒー / 白湯 / 風呂を沸かす等）を既定から削除し、誰にでも当てはまる汎用アクションだけをジャンル別カタログに残す。これは [K-031](../../KNOWLEDGE.md)（成熟した自分の運用をそのまま初期値にしない＝最小核まで削る）の適用。完成形（朝/夜の完成ルーティン）は「おすすめカテゴリ」のプレビューとしてゴール提示に回す。
- **moment を束から外し、採用単位を個別アクションにすることで「軸の混在」を解消した。** 旧モデルは「ジャンル（goal）」と「時間帯（moment）」を 1 つの束に畳んでいたため、同じアクションが朝/夜束に重複定義されていた。ジャンル別カテゴリは genre 1 軸で各アクション 1 つに正規化し、moment はチェーン側（採用後）に分離する。CLAUDE.md §正準データ「分類軸の独立性」/ [K-030](../../KNOWLEDGE.md) と整合。
- **catalog/live 分離（[ADR-0030](0030-template-module-link-data-model.md) の中核）は維持した。** 採用は「catalog を読んで live を生成する一方向変換」のまま。置換するのは catalog の内部構造（module → category）であり、live の正準データ不変条件（[ADR-0001](0001-chain-data-model.md)）は触らない。
- **採用時フィルタを「UI フィルタ」ではなく「カタログ構造が既定集合を決める」形にした。** ユーザーに評価軸スライダーのような操作を要求せず（Augmentation 原則）、カタログを汎用のみに棚卸しすること自体が「採用時に出る既定集合」を形作る。「初期全選択 + 外したいものだけ外す」は [K-030](../../KNOWLEDGE.md)（UI に出すのは permission ではなく state）と同型。
- トレードオフとして捨てたもの: (a) エビデンス可視性をカタログメタとして持つこと（→ お蔵入り。監査 [docs/template-audit.md](../template-audit.md) の 3 軸は新規アクション追加時の人間の照合チェックとして残し、データカラムにはしない）、(b) テーマ束単位の一括採用（→ おすすめカテゴリの「初期全選択」で代替）。

## 想定される影響

- **後続トラックがこの構造に依存する**: (1) catalog/schema 再構成（module/links → category モデルへの DDL 変更 + v0 seed 差し替え）、(2) 採用フロー（onboarding [ADR-0031](0031-onboarding-first-run-route.md) / [ADR-0035](0035-onboarding-action-selection.md) / discovery）の category 対応。内容（v0 カタログ）が本 ADR で固まったので着手可能。
- **[ADR-0030](0030-template-module-link-data-model.md) との関係（[K-005](../../KNOWLEDGE.md) / [K-015](../../KNOWLEDGE.md) 適用）**: catalog/live 分離原則は継承するため ADR-0030 を全面 supersede しない。ただし ADR-0030 の `modules` / `links` スキーマと採用単位（束ベース）の前提は本 ADR で置換される。ADR-0030 側に逆参照 1 行を追加し、単体読みで誤誘導されないようにする（双方向リンク）。
- **[docs/template-modules-spec.md](../template-modules-spec.md) §2-§9 は module モデル前提**なので、本 ADR と矛盾する。spec 本体の書き換えは catalog/schema 再構成トラックの責務とし、本 ADR では §先頭に「module モデルは ADR-0039 で category 2型に置換」の注記を入れて誤誘導を防ぐ（[spec-sync ルール](../../.claude/rules/spec-sync.md) / [K-005](../../KNOWLEDGE.md)）。
- **マイグレーションコスト**: 現行 v0 catalog seed（`src/catalogSeed.ts`）は module/links ベース。検証期間（[ADR-0022](0022-phase-1-completion-and-verification-operation.md)）の運用データは live 側（採用済みチェーン）なので catalog 再構成の影響を受けない（catalog/live 分離の恩恵）。catalog テーブルの再構成は [ADR-0027](0027-non-destructive-migration.md) の ALTER ベース migration で対応する。
- **覆す場合のコスト**: 構造（module→category）を再度覆すと catalog/schema 再構成・onboarding・discovery の全実装トラックに波及する（Issue #117 全体）。逆に v0 カタログの内容（アクションの増減・カテゴリ追加）は seed の差分なので低コストで調整できる（[docs/template-audit.md](../template-audit.md) の 3 軸照合に通すこと）。
