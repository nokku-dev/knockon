# iOS Simulator QA チェックリスト (v1 提出前)

> 対象: App Store 提出前の網羅的動作確認
> 実行環境: iPhone 16 Pro Max Simulator (iOS 18.6) / Xcode 16.4 / macOS 26.5
> 検証ビルド: `eas build --profile preview --platform ios` (simulator 向け `.app`)
> 上位 SoT: [app-store-submission.md](app-store-submission.md)

## 実行結果サマリ (2026-07-30)

検証ビルド: iOS `c385f615` (commit `2567f66`) → 修正後 `c0bb7185` (commit `256dbbc`) で再検証。
Android `3d646eef` (commit `b1d7efd`) で残項目を実機確認。
iPhone 16 Pro Max Simulator。

**修正後の再ビルドで J-4 と L-5 を確認済み**（直した箇所を未検証のまま production に進めない）。

- **検証済み: 75 項目**
- **保留 (データ / 環境前提): 7 項目**（M-1〜M-6 と G-6）
- **未実施: 0 項目**（残っていた 5 件は 2026-07-31 に **Android 実機**で確認。§N 参照）
- **発見した不具合: 2 件** → いずれも修正済み（PR #271 / PR #274）（`initAnalytics()` の初期化順序により
  `PostHogProvider` が無効なクライアントを生成し `captureAppLifecycleEvents` が失われていた。
  型チェックもテストも通っており、Simulator のデバイスログを読んで初めて観測できた）
  および **#273**（永続化する時刻 5 箇所が UTC 保存。特に `settlement_retractions.retracted_at` は
  `lastRetractionDateForNode` がローカル日付と比較するため**定着判定が狂う**。DB を直読みして発見。
  PR #274 で修正 + `localTimestampGuard.test.ts` で機械的に再発防止）

**残る 7 項目の保留はいずれもデータ / 環境前提**（定着★は 14D 窓に 10 日分の達成が必要、
通知配信とジオフェンスは実機のみ）で、各項目に理由を明記した。

**提出前に検証できることは検証し尽くした。**

## 前提と限界

**実機がないため Simulator で代替する**（[TODO.md](../../TODO.md) §iOS）。Simulator で意味を持つ検証と、持たない検証を明確に分ける。

| Simulator で検証できる | できない (実機のみ) |
| --- | --- |
| 権限 usage description の欠落 (iOS が同様にプロセスを落とす) | バックグラウンドでの通知配信 |
| レイアウト / 画面遷移 / DB 永続化 | ジオフェンス到達判定 |
| ローカル通知のスケジュール | 実機の描画性能・電池消費 |
| 分析イベントの送信 | Dynamic Island 実挙動 |

**データ前提が必要で当セッションでは検証不能な項目**は「⏸ 保留」として理由を明記する（例: 定着は 14D 窓に 10 日分の達成が必要）。

## 検証方法の補足

- **UI 操作**: maestro (座標タップ)。RN のテキストは a11y 階層に露出しないため text マッチは不可（システムダイアログは可）
- **DB 直接検証**: `xcrun simctl get_app_container <udid> co.nokku.knockon data` → `Documents/SQLite/knockon.db` を `sqlite3` で読む
- **ビルド成果物の検証**: `plutil -p knockon.app/Info.plist`
- **送信の検証**: `xcrun simctl spawn <udid> log show` でネットワーク接続とコンソールを見る

### 実行して分かった知見

- **DB 直読みが最も費用対効果が高い**。画面を見るだけでは「保存された」で終わるが、DB を見ると
  値の中身まで確定する。#273 の UTC 保存はこれで発見した（画面上は正常に見えていた）
- **座標タップは脆い**。要素の表示/非表示（チェックリストの dismiss 等）でレイアウトが動くと全て外れる。
  **`accessibilityLabel` があれば `tapOn: text:` の方が確実**（RN の `Text` 中身は a11y 階層に出ないが、
  `accessibilityLabel` を明示した要素は拾える）
- **キーボードが最大の障害**。入力後にモーダルの位置が変わる / ボタンがキーボードに覆われる。
  `hideKeyboard` を挟んでも多段フォームは安定しなかった（F-4）

---

## A. ビルド成果物 / 設定

- [x] **A-1** `Info.plist` に `NSLocationWhenInUseUsageDescription` が日本語で存在
- [x] **A-2** `Info.plist` に `NSLocationAlwaysAndWhenInUseUsageDescription` / `NSLocationAlwaysUsageDescription` が日本語で存在（英語の既定文言が漏れていない）
- [ ] **A-3** `UIBackgroundModes` に `location` が**存在**（#301 で `isIosBackgroundLocationEnabled` を立てた。無いと `startGeofencingAsync` が throw する）— ⚠ 次のビルドで再確認が要る
- [x] **A-4** `CFBundleIdentifier` = `co.nokku.knockon`
- [x] **A-5** `CFBundleShortVersionString` = `1.0.0`
- [x] **A-6** `ITSAppUsesNonExemptEncryption` = `false`
- [x] **A-7** `SEED_SCREENSHOT_DATA` = `false`（ダミーデータが出荷に混入しない）

## B. 起動 / 初期化

- [x] **B-1** 初回起動でクラッシュしない
- [x] **B-2** 初回起動で `/onboarding` に誘導される
- [x] **B-3** アプリ再起動後もデータが保持される（DB 永続化）
- [x] **B-4** 再起動時にオンボーディングが再表示されない（`onboarding_completed` フラグ）

## C. オンボーディング

- [x] **C-1** 1/7「続けるを、積み上げる。」→「はじめる」で進む
- [x] **C-2** 2/7「いつを良くしたい?」朝 / 夜 を選択できる
- [x] **C-3** 3/7 時刻選択（既定 07:00）→「次へ」
- [x] **C-4** 4/7 アクション選択
- [x] **C-5** 5/7「もう一方も始めますか?」→「1本で始める」
- [x] **C-6** 6/7「通知を許可」→ OS ダイアログ → 許可できる
- [x] **C-7** 7/7「準備完了」→「今すぐ1回試す」で Today へ
- [x] **C-8** 「あとで」で通知をスキップしても進める（**Android 実機**で確認）

## D. Today（コアループ）

- [x] **D-1** チェーンカードが表示される（タイトル / 進捗 0/8 / 発火中ピル）
- [x] **D-2** カードタップで ChainDetail (Bottom Sheet) が開く
- [x] **D-3** ノードのドットタップで達成になる（塗りに変わる）
- [x] **D-4** 達成に応じてスパインが伸びる（ADR-0010 達成済みノード範囲）
- [x] **D-5** 再タップで達成を取り消せる（DB で `achieved` が 1 → 0。**削除ではなく bool 更新** =
      ADR-0001 の正準形どおり。進捗リングも 2/8 → 1/8 に追随）
- [x] **D-6** Today 見出しの「定着 N 個」は **0 件のとき非表示**（`TodayScreen.tsx` に
      「settledCount=0 は非表示」と ADR-0050 根拠つきで実装。当初この項目を「0 件でも表示」と
      書いたのは誤りだった。定着発生後の表示は M-1 と同じ理由で保留）
- [x] **D-7** ノード長押しでメモ作成モーダルが出る。**長押ししたノードが自動選択**される
      （ADR-0044）。`notes` に保存されることを DB で確認（研究タブ非表示でも作成は可 = ADR-0049）
- [x] **D-8** 空状態で「まだチェーンがありません。「チェーン」タブから新規作成してください。」+
      「+ チェーンを新規作成」が出る

## E. 立ち上げチェックリスト (#165)

- [x] **E-1** Today 上部に表示され「1本目のチェーンを採用した」が達成済み
- [x] **E-2** ノード達成で「はじめてのノードを達成した」が進む（1/5 → 2/5）
- [x] **E-3** × で dismiss でき、`app_settings.checklist_dismissed_at` が入る

## F. チェーン CRUD

- [x] **F-1** ChainDetail の「編集」から編集画面が開く
- [x] **F-2** 起点アンカーが なし / 時刻 / 場所 の 3 種から選べる
- [x] **F-3** 「場所」選択で「現在地を取得」と発火半径 (50/100/200m) が出る
- [x] **F-4** 新規チェーンを作成できる（**Android 実機**で確認。Simulator では UI 自動化の限界で
      再現できず、手動で埋めた）
- [x] **F-5** ノードを削除できる
- [x] **F-6** ノードの ON/OFF（一時停止）が切り替わる（**Android 実機**で確認）
- [x] **F-7** ステータスを**双方向**に切り替えられる（`active` → `stocked` → `active` を DB で確認）。
      チェーン一覧の「アクティブ (N) / 休止中 (N)」フィルタも追随する
- [x] **F-8** 一時休止にしたチェーンが Today に出ない（空状態に切り替わる）
- [x] **F-9** チェーンを削除できる（**Android 実機**で確認）

## G. ログ（定着ポートフォリオ + メトリクス）

- [x] **G-1** ログタブが開く
- [x] **G-2** ステージ見出しが **3 段**（育成中 / もう少しで定着 / 定着）で、「これから」が**無い**（ADR-0051）
- [x] **G-3** 0 件でも見出しが表示される
- [x] **G-4** メトリクスを手入力できる（体重）
- [x] **G-5** **#109 検証**: 記録した日付が当日になる（DB の `recorded_at` がローカル壁時計）
- [ ] ⏸ **G-6** メトリクスのトレンドグラフ — 1 点では線が描けない。複数日のデータが必要なため保留
      （最新値 `62.5 kg` と「14 日で 1 件記録」の表示は確認済み）
- [x] **G-7** メトリクス種別編集が開き、**Notion 連携キー入力欄が無い**（#184）
- [x] **G-8** 60D 達成マトリクスが**表示されない**（ADR-0051 で撤去）

## H. 設定

- [x] **H-1** 歯車から設定モーダルが開く
- [x] **H-2** 1日の区切り時刻（reset time）を変更できる（ADR-0028）
- [x] **H-3** **テーマ選択 UI が非表示**（`SHOW_THEME_SELECTOR = false`・Light 未移行）
- [x] **H-4** チェーンのエクスポート導線が非表示（レビュー中の設定）

## I. 権限

- [x] **I-1** 位置権限のダイアログが出て、**アプリがクラッシュしない**（#254 の核心）
- [x] **I-2** ダイアログ文言が「アンカーとして登録する場所の座標を取得するために使用します。」
- [x] **I-3** 選択肢に「**常に許可**」が**出ない**（Always を要求していない）
- [x] **I-4** 通知権限を許可できる
- [x] **I-5** 位置権限を**拒否**してもアプリが動作する（**Android 実機**で確認。ADR-0003 の劣化動作）

## J. 分析（PostHog / ADR-0053）

- [x] **J-1** `eu.i.posthog.com/batch/` への送信が発生する（EU リージョン）
- [x] **J-2** ノードタップ直後に送信がフラッシュされる（`node_completed`）
- [x] **J-3** apiKey 未設定の警告が出ない（設定が読めている）
- [x] **J-4** `You must pass your PostHog project's api key` エラーが**出ない** — PR #271 を含む
      再ビルド (`c0bb7185` / commit `256dbbc`) で確認。`/batch/` 送信は継続
- [x] **J-5** 送信ペイロードに文字列プロパティが含まれない — **型で担保**（`SafeValue = number | boolean`）+
      `analyticsEvents.test.ts` で実行時検証。送信本文の傍受は proxy が必要なため未実施だが、
      文字列を渡すコードはコンパイルを通らない
- [x] **J-6** 画面名が正規化されている — `normalizeScreenPath` の単体テストで担保
      （`/chain/<id>` → `chain_detail`）

## K. リリーススコープ / 禁止 UI

- [x] **K-1** タブが **Today / チェーン / ログ の 3 本**
- [x] **K-2** **研究タブが表示されない**（ADR-0049）
- [x] **K-3** ノード行に **7D ミニマトリクスが無い**（ADR-0051）
- [x] **K-4** 連続日数（ストリーク）表示が**どこにも無い**（ADR-0036）
- [x] **K-5** 炎 / 紙吹雪 / ヒートマップ / カレンダー型グリッドが**無い**（CLAUDE §4 禁止 UI）
- [x] **K-6** 未達を指差す赤字・アラート表示が**無い**

## L. データ永続化 / スキーマ

- [x] **L-5** **#274 検証**: `notes.created_at` がローカル壁時計。同一テーブル内で修正前後を並べて確認
      （旧ビルド `06:35:40` = UTC / 新ビルド `16:15:08` = ローカル。実時刻はローカル 16:15 / UTC 07:15）

- [x] **L-1** 達成記録が `achievements` に保存されている
- [x] **L-2** `metrics.recorded_at` がローカル壁時計形式（**#109**）
- [x] **L-3** 派生値のカラムが存在しない（定着 / 達成率 / streak を保存していない・ADR-0001）
- [x] **L-4** 旧 module/link 系テーブルが存在しない（#160 で撤去済み）

## N. Android 実機で埋めた項目 (2026-07-31)

iOS 実機が無いため、**Simulator の UI 自動化で埋められなかった項目を Android 実機で確認**した。
これらは分析ラッパー・イベント計装・タイムスタンプ処理と同じく**プラットフォーム非依存の JS ロジック**
なので、Android で通れば iOS でも同じ結果になる。iOS 固有（Info.plist / 権限ダイアログ / タブ構成）は
Simulator で確定済み。

検証ビルド: `3d646eef` (commit `b1d7efd`)。**修正 3 件すべてを含む**
（#271 初期化順序 / #274 タイムスタンプ / ADR-0054 長押しメモ無効化）。

- [x] **N-1** C-8 / F-4 / F-6 / F-9 / I-5 を実機で確認（上記各項目に反映済み）
- [x] **N-2** **ADR-0054**: ノード長押しでメモモーダルが**出ない**
- [x] **N-3** 起動 → バックグラウンド → 復帰を繰り返してクラッシュしない（#271 の実挙動確認）

## M. 保留（当セッションでは検証不能）

- [ ] ⏸ **M-1** 定着（★）表示 — 14D 窓に 10 日分の達成が必要。DB を直接操作すれば可能だが、正規の経路ではないため保留
- [ ] ⏸ **M-2** 定着取り下げ — M-1 の前提が必要
- [ ] ⏸ **M-3** 夜の達成サマリー通知（21:00・ADR-0042）— 時刻依存
- [ ] ⏸ **M-4** 時刻アンカーの自動発火 — 07:00 まで待つ必要がある
- [ ] ⏸ **M-5** ジオフェンス到達判定 — 実機のみ（#301 で実装済み。Simulator では region monitoring が発火しない）
- [ ] ⏸ **M-6** バックグラウンド通知配信 — 実機のみ
